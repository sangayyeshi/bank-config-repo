server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/account_db
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Xangay@12}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
