server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/notification_db
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Xangay@12}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: sangayyeshi19@gmail.com
    password: ${XangDen@11}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}

  instance:
    prefer-ip-address: true
